import 'package:flutter/cupertino.dart';

class LevelButtons{
  String name;
  Color color;
  int numberOfStars;
  Widget goTo;

  LevelButtons({this.name, this.color, this.numberOfStars, this.goTo});
}